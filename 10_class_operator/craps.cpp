//class Craps implementation

#include <vector>

	void play_game() 
	{

	}

	Craps operator << (vect<Roll>) 
	{
		for (int i = 0; i < vect.size(); i++) {
			for (int j = 0; j < vect[i].size(); j++)
			{
				cout << vect[i][j] << " ";
			}
	}

	Craps operator >> (vect<Shooter>) 
	{
		int userinput;
		cout << "Enter number of shooters ";
		cin >> userprompt;

		while (userinput > 0)
		{
			for (int i = 0; i < useriput; i++)
			{
				shooter.push_back(vect);
			}
		}

		return 0;
	}